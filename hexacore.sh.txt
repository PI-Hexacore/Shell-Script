#!/bin/bash

# ==============================================
# SCRIPT DE AUTOMAÇÃO HEXACORE (BACK + DB + FRONT)
# ==============================================

echo "============================="
echo " Iniciando Setup da Hexacore "
echo "============================="

# -----------------------------
# 1. Verifica se o Git está instalado
# -----------------------------
git --version > /dev/null 2>&1
if [ $? != 0 ]; then
  echo "Git não encontrado. Instalando..."
  sudo apt update
  sudo apt install git -y
fi

# -----------------------------
# 2. Verifica e instala Java + Maven (para backend)
# -----------------------------
java -version > /dev/null 2>&1
if [ $? != 0 ]; then
  echo "Java não está instalado. Instalando Java + Maven..."
  sudo apt update
  sudo apt install openjdk-21-jre maven -y
fi

# -----------------------------
# 3. Verifica e instala Node.js + npm (para frontend)
# -----------------------------
node -v > /dev/null 2>&1
if [ $? != 0 ]; then
  echo "Node.js não está instalado. Instalando..."
  sudo apt update
  sudo apt install nodejs npm -y
fi

# -----------------------------
# 4. Verifica Docker
# -----------------------------
docker --version > /dev/null 2>&1
if [ $? != 0 ]; then
  echo "Docker não está instalado. Instalando Docker..."
  sudo apt update
  sudo apt install docker.io -y
  sudo systemctl start docker
  sudo systemctl enable docker
fi

# -----------------------------
# 5. Permissão Docker
# -----------------------------
if ! groups $USER | grep -q '\bdocker\b'; then
  echo "Adicionando usuário ao grupo Docker..."
  sudo usermod -aG docker $USER
  echo "⚠️ Saia e entre novamente no sistema, depois rode o script de novo."
  exit 0
fi

# -----------------------------
# 6. Clona ou atualiza os repositórios
# -----------------------------
BASE_DIR=Hexacore_Project
mkdir -p $BASE_DIR
cd $BASE_DIR

echo "📦 Verificando repositórios..."

update_or_clone() {
  local REPO_URL=$1
  local FOLDER_NAME=$2

  if [ -d "$FOLDER_NAME/.git" ]; then
    echo "🔄 Atualizando $FOLDER_NAME..."
    cd $FOLDER_NAME
    git pull
    cd ..
  else
    echo "⬇️ Clonando $FOLDER_NAME..."
    git clone $REPO_URL
  fi
}

update_or_clone "https://github.com/PI-Hexacore/Java" "Java"
update_or_clone "https://github.com/PI-Hexacore/BdHexacore" "BdHexacore"
update_or_clone "https://github.com/PI-Hexacore/Site_Hexacore" "Site_Hexacore"

echo "✅ Repositórios sincronizados!"


# -----------------------------
# 7. Banco de Dados
# -----------------------------
CONTAINER_DB="hexacore-db"
DB_ROOT_PASS="142536"
DB_NAME="hexacore"

echo "🐬 Criando container do banco de dados..."

docker stop $CONTAINER_DB 2>/dev/null
docker rm $CONTAINER_DB 2>/dev/null

docker run -d \
  --name $CONTAINER_DB \
  -e MYSQL_ROOT_PASSWORD=$DB_ROOT_PASS \
  -e MYSQL_DATABASE=$DB_NAME \
  -p 3306:3306 \
  mysql:8.0

echo "✅ Banco de dados rodando!"

# -----------------------------
# 8. Backend 
# -----------------------------
echo "🛠 Subindo backend (modo simples igual ao BD)..."

BACKEND_DIR="$BASE_DIR/Java/Hexacore-backend-java"

docker stop hexacore-backend 2>/dev/null
docker rm hexacore-backend 2>/dev/null

docker run -d \
  --name hexacore-backend \
  -p 8080:8080 \
  -v "$BACKEND_DIR":/app \
  -w /app \
  maven:3.9.6-eclipse-temurin-21 \
  bash -c "mvn clean package -DskipTests && java -jar /app/target/*.jar"

echo "✅ Backend rodando!"

# -----------------------------
# 9. Frontend
# -----------------------------
echo "🌐 Subindo frontend (modo simples igual ao BD)..."

FRONT_DIR="$BASE_DIR/Site_Hexacore"

docker stop hexacore-frontend 2>/dev/null
docker rm hexacore-frontend 2>/dev/null

# Build React dentro do container Node
docker run --rm \
  -v "$FRONT_DIR":/app \
  -w /app \
  node:18 \
  bash -c "npm install && npm run build"

# Servir com Nginx
docker run -d \
  --name hexacore-frontend \
  -p 80:80 \
  -v "$FRONT_DIR/build":/usr/share/nginx/html \
  nginx:latest

echo "✅ Frontend rodando!"

# -----------------------------
# 10. Resumo final
# -----------------------------
echo ""
echo "=========================================="
echo "🚀 Setup concluído com sucesso!"
echo "Banco de Dados: rodando na porta 3306"
echo "Backend: rodando na porta 8080"
echo "Frontend: rodando na porta 80"
echo "=========================================="
